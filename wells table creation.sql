CREATE TABLE wells (
    id int primary key,
    status_group varchar,
    amount_tsh float,
    funder varchar,
    gps_height int,
    installer varchar,
    longitude float,
    latitude float,
    basin varchar,
    region varchar,
    lga varchar,
    ward varchar,
    population int,
    public_meeting varchar,
    permit varchar,
    extraction_type varchar,
    management varchar,
    payment varchar,
    water_quality varchar ,
    quantity varchar,
    source varchar,
    waterpoint_type varchar
);
